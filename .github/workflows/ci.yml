name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    name: Build, Lint & Unit Tests
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Set up Go
        uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Install tools
        run: |
          go install golang.org/x/tools/cmd/goimports@v0.42.0
          go install golang.org/x/vuln/cmd/govulncheck@v1.1.4

      - name: Verify dependencies
        run: |
          go mod tidy
          git diff --exit-code go.mod go.sum

      - name: Check doc/config consistency
        run: python3 scripts/check_doc_config_consistency.py

      - name: Install Atlas
        uses: ariga/setup-atlas@cf119a6512b9133214e3a98bd4f731bfc413e9cb
        with:
          version: v1.1.0

      - name: Verify migration integrity
        run: |
          atlas migrate validate --dir file://migrations

      - name: Build
        run: go build -ldflags "-X main.version=ci-${GITHUB_SHA::8}" -o bin/akashi ./cmd/akashi

      # Guards the zero-infra SQLite MCP binary (ADR-009), which is built with
      # -tags lite and so excludes every !lite file. Without this step the lite
      # build rots silently when shared code starts referencing a cloud-only
      # symbol (it did, undetected, until the conflicts/storage relocation).
      - name: Build lite (akashi-local)
        run: go build -tags lite -o /dev/null ./cmd/akashi-local

      - name: Lint
        uses: golangci/golangci-lint-action@9fae48acfc02a90574d7c304a1758ef9895495fa # v7
        with:
          version: v2.11.0

      - name: Vet
        run: go vet ./...

      - name: Security scan
        # GO-2026-4887 (docker/docker AuthZ bypass) has no fix available and
        # is only reachable through testcontainers test infrastructure.
        # Run govulncheck for visibility but don't block CI on unfixable
        # transitive vulnerabilities.  Re-evaluate when docker/docker ships
        # a patched release.
        continue-on-error: true
        run: govulncheck ./...

      # Fuzzing is NOT on the merge path. `go test` without -fuzz runs every
      # fuzz target's seed corpus as ordinary subtests (testing/fuzz.go, the
      # "Fuzzing is not enabled" branch), so the step below already gives
      # deterministic, race-detected crash coverage of every seed and of any
      # crasher committed under testdata/fuzz/.
      #
      # Timed fuzzing (-fuzztime=<duration>) additionally spuriously fails on
      # go1.26 — golang/go#75804, a race between the coordinator's deadline
      # context and the child context its error suppression compares against.
      # The fix (CL 774140) is in master and go1.27 only, and there is no
      # go1.26 backport. Five months and seven remediation commits produced
      # zero findings and repeated red builds on doc-only PRs.
      # Mutation fuzzing now runs in .github/workflows/fuzz-nightly.yml with a
      # count-based budget, which never constructs the deadline context.
      - name: Unit tests
        run: go test -race -count=1 ./...

  integration:
    name: Integration Tests
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Set up Go
        uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Integration tests
        run: go test -race -count=1 -tags integration -coverprofile=coverage.out ./...

      - name: Enforce coverage threshold
        run: bash scripts/check_coverage.sh coverage.out 50

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: coverage-report
          path: coverage.out

      - name: Upload coverage to Codecov
        if: always()
        continue-on-error: true
        uses: codecov/codecov-action@b9fd7d16f6d7d1b5d2bec1a2887e65ceed900238 # v4
        with:
          files: coverage.out
          fail_ci_if_error: false

  build-ui:
    name: Build with UI
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Set up Go
        uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Set up Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: '22'

      - name: Build frontend
        run: cd ui && npm ci && npm run build

      - name: Build Go binary with UI embed
        run: go build -tags ui -ldflags "-X main.version=ci" -o /dev/null ./cmd/akashi

  test-go-sdk:
    name: Test Go SDK
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Set up Go
        uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Run Go SDK tests
        run: cd sdk/go && go test -race -count=1 ./...

  test-python-sdk:
    name: Test Python SDK
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: '3.11'

      - name: Install dependencies and run tests
        run: cd sdk/python && pip install -e ".[dev]" && pytest

  test-typescript-sdk:
    name: Test TypeScript SDK
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Set up Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: '22'

      - name: Install dependencies and run tests
        run: cd sdk/typescript && npm ci && npm test

  verify-exit-criteria:
    name: Verify Exit Criteria
    runs-on: ubuntu-latest
    services:
      timescaledb:
        image: timescale/timescaledb:latest-pg18
        env:
          POSTGRES_USER: akashi
          POSTGRES_PASSWORD: akashi
          POSTGRES_DB: akashi
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U akashi -d akashi"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgres://akashi:akashi@localhost:5432/akashi?sslmode=disable
      MAX_DEAD_LETTERS: "0"
      MAX_OUTBOX_OLDEST_SECONDS: "1800"
      STRICT_RETENTION_CHECK: "false"
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Install Atlas
        uses: ariga/setup-atlas@cf119a6512b9133214e3a98bd4f731bfc413e9cb
        with:
          version: v1.1.0

      - name: Bootstrap extensions
        run: |
          psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -c "CREATE EXTENSION IF NOT EXISTS vector;"
          psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -c "CREATE EXTENSION IF NOT EXISTS timescaledb;"

      - name: Apply migrations
        run: |
          atlas migrate apply --dir file://migrations --url "$DATABASE_URL"

      - name: Verify durability exit criteria
        run: |
          make verify-exit-criteria

  # The same history as verify-exit-criteria, applied by ptah-compat instead of
  # the Atlas CLI. ptah-compat reads atlas.hcl and atlas.sum and writes the same
  # atlas_schema_revisions table, so the last Atlas step is the check that the
  # two tools agree on what was applied.
  migrations-ptah-compat:
    name: Migrations (ptah-compat)
    runs-on: ubuntu-latest
    services:
      timescaledb:
        image: timescale/timescaledb:latest-pg18
        env:
          POSTGRES_USER: akashi
          POSTGRES_PASSWORD: akashi
          POSTGRES_DB: akashi
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U akashi -d akashi"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgres://akashi:akashi@localhost:5432/akashi?sslmode=disable
      MAX_DEAD_LETTERS: "0"
      MAX_OUTBOX_OLDEST_SECONDS: "1800"
      STRICT_RETENTION_CHECK: "false"
      # Pinned like Atlas. v0.8.1 is the first release that keeps the leading
      # zeros of a revision version and ignores the schemas timescaledb
      # creates in the clean gate; this job depends on both.
      PTAH_COMPAT_VERSION: v0.8.1
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Set up Go
        uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version-file: go.mod

      # setup-go sets GOTOOLCHAIN=local, and the ptah module needs a newer Go
      # than go.mod selects. auto lets go install fetch that toolchain, which
      # the checksum database verifies like the module.
      - name: Install ptah-compat
        env:
          GOTOOLCHAIN: auto
        run: go install "ptah.run/cmd/ptah-compat@${PTAH_COMPAT_VERSION}"

      - name: Install Atlas
        uses: ariga/setup-atlas@cf119a6512b9133214e3a98bd4f731bfc413e9cb
        with:
          version: v1.1.0

      - name: Bootstrap extensions
        run: |
          psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -c "CREATE EXTENSION IF NOT EXISTS vector;"
          psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -c "CREATE EXTENSION IF NOT EXISTS timescaledb;"

      - name: Validate migrations with ptah-compat
        run: make migrate-validate ATLAS=ptah-compat

      - name: Apply migrations with ptah-compat
        run: make migrate-apply ATLAS=ptah-compat

      - name: Atlas reads the history ptah-compat wrote
        run: |
          atlas migrate status --dir file://migrations --url "$DATABASE_URL" | tee status.txt
          grep -q 'Migration Status: OK' status.txt
          grep -q 'Pending Files:   0' status.txt

      - name: Verify durability exit criteria
        run: |
          make verify-exit-criteria
